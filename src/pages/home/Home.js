import styled from "styled-components";

const Container = styled.section``;
const Banner = styled.div``;

const Home = () => {
  return (
    <Container>
      <Banner></Banner>
    </Container>
  );
};

export default Home;
